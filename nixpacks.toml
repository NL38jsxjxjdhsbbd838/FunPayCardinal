[phases.setup]
nixPkgs = ["python312", "gcc", "glibc", "zlib"]

[phases.install]
cmds = [
  "python -m venv /opt/venv",
  ". /opt/venv/bin/activate",
  "pip install -r requirements.txt"
]

[start]
cmd = "python main.py"
