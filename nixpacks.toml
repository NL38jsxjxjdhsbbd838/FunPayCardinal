[phases.setup]
nixPkgs = ["python312", "gcc", "glibc", "zlib", "libffi", "openssl", "freetype", "libpng", "pkg-config"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "bash start.sh"
